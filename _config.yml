collections:
  projects:
    output: true
  others:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: "base"
  - scope:
      path: ""
      type: "projects"
    values:
      layout: "project-page"
  - scope:
      path: ""
      type: "others"
    values:
      layout: "other-page"